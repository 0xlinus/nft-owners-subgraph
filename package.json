{
  "name": "nft-owners",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ nft-owners",
    "deploy:linus": "graph deploy --product hosted-service 0xlinus/nfts"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "0.24.0",
    "@graphprotocol/graph-cli": "0.24.0"
  }
}
